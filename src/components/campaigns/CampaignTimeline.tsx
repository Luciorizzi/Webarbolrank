import type { CampaignUpdate } from "@/types/campaign";
const formatter = new Intl.DateTimeFormat("es-AR", { timeZone: "UTC" });
export function CampaignTimeline({ updates }: { updates: CampaignUpdate[] }) { return <ol className="relative border-l border-white/10 pl-7">{updates.map((update) => <li key={update.id} className="relative pb-9 last:pb-0"><span className="absolute -left-[2.05rem] top-1 size-3 rounded-full border-2 border-[#080d0b] bg-emerald-400" /><time className="text-xs uppercase tracking-wider text-zinc-500">{formatter.format(new Date(update.date))}</time><h3 className="mt-2 font-bold text-white">{update.title}</h3><p className="mt-1 text-sm leading-6 text-zinc-500">{update.content}</p></li>)}</ol>; }
