import { formatARS } from "@/lib/currency";
import type { RankingParticipant } from "@/types/ranking";

export function DonationSummary({ participant, donorName, anonymous, trees, total, onEdit }: { participant: RankingParticipant | null; donorName: string; anonymous: boolean; trees: number; total: number; onEdit: () => void }) {
  return <div><p className="eyebrow">Resumen de donación</p><dl className="my-6 space-y-4 border-y border-white/8 py-5"><div><dt className="text-xs uppercase tracking-wider text-zinc-500">{participant ? "Apoyás a" : "Destino"}</dt><dd className="mt-1 text-xl font-bold text-white">{participant?.name ?? "Donación general"}</dd></div><div><dt className="text-xs uppercase tracking-wider text-zinc-500">Donante</dt><dd className="mt-1 text-white">{anonymous ? "Anónimo" : donorName}</dd></div><div className="flex justify-between"><dt className="text-zinc-500">Árboles</dt><dd className="text-white">{trees}</dd></div><div className="flex justify-between"><dt className="text-zinc-500">Total final</dt><dd className="font-bold text-emerald-300">{formatARS(total)}</dd></div><p className="text-xs text-zinc-500">Sin costos adicionales.</p></dl><button type="button" disabled className="w-full cursor-not-allowed rounded-full bg-emerald-400 px-5 py-4 text-xs font-bold uppercase tracking-wide text-emerald-950 opacity-60">Pago disponible próximamente</button><button type="button" onClick={onEdit} className="mt-3 w-full rounded-full border border-white/10 px-5 py-3 text-sm text-white hover:border-white/25">Volver / Editar</button></div>;
}
