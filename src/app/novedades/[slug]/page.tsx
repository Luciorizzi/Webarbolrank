import { notFound } from "next/navigation";
import { CampaignStatusBadge } from "@/components/campaigns/CampaignStatusBadge";
import { CampaignTimeline } from "@/components/campaigns/CampaignTimeline";
import { findCampaignBySlug } from "@/data/campaigns";
import { formatARS } from "@/lib/currency";
const df = new Intl.DateTimeFormat("es-AR", { timeZone: "UTC" });
export default async function Page({ params }: { params: Promise<{ slug: string }> }) { const { slug } = await params; const campaign = findCampaignBySlug(slug); if (!campaign) notFound(); return <main className="mx-auto max-w-6xl px-5 py-20 sm:px-8"><CampaignStatusBadge status={campaign.status} /><h1 className="mt-5 max-w-4xl font-display text-5xl font-black uppercase text-white sm:text-7xl">{campaign.title}</h1><p className="mt-5 max-w-2xl text-lg leading-8 text-zinc-400">{campaign.description}</p><section className="mt-10 grid gap-px overflow-hidden rounded-3xl border border-white/8 bg-white/8 sm:grid-cols-2 lg:grid-cols-4">{[["Ubicación", campaign.location], ["Organización", campaign.ngoName], ["Meta", formatARS(campaign.goalAmount)], ["Recaudado", formatARS(campaign.raisedAmount)], ["Árboles", String(campaign.treeGoal)], ["Actividad", campaign.plantingDate ? df.format(new Date(campaign.plantingDate)) : "A confirmar"]].map(([label, value]) => <div key={label} className="bg-[#0b100e] p-5"><p className="text-xs uppercase tracking-wider text-zinc-500">{label}</p><p className="mt-2 font-bold text-white">{value}</p></div>)}</section><div className="mt-16 grid gap-14 lg:grid-cols-[1fr_.7fr]"><section><h2 className="mb-8 font-display text-3xl font-bold uppercase text-white">Cronología</h2><CampaignTimeline updates={campaign.updates} /></section><section><h2 className="font-display text-3xl font-bold uppercase text-white">Evidencia</h2>{campaign.evidence.length ? <ul className="mt-6 space-y-3">{campaign.evidence.map((item) => <li key={item} className="rounded-2xl border border-white/8 bg-white/[0.025] p-4 text-sm text-zinc-400">{item}</li>)}</ul> : <p className="mt-5 text-zinc-500">La campaña sigue abierta. La evidencia se publicará con cada actualización.</p>}<p className="mt-5 text-xs text-zinc-600">Contenido demostrativo hasta conectar datos reales.</p></section></div></main>; }
