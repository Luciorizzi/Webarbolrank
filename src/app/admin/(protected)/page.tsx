import Link from "next/link";
import { getDashboardData } from "@/lib/data/admin/dashboard";
import { formatARS as formatCurrency } from "@/lib/currency";

export default async function AdminDashboard() { const { metrics, recent } = await getDashboardData(); const cards = [["Kg aprobados", `${metrics.approvedKg.toLocaleString("es-AR")} kg`], ["Monto aprobado", formatCurrency(metrics.approvedAmount)], ["Aprobadas", metrics.approved], ["Pendientes", metrics.pending], ["Rechazadas", metrics.rejected], ["Reembolsadas", metrics.refunded], ["Participantes activos", metrics.activeParticipants], ["Campañas activas", metrics.activeCampaigns]]; return <section><h1 className="text-3xl font-black text-white">Dashboard</h1><div className="mt-6 grid gap-3 sm:grid-cols-2 lg:grid-cols-4">{cards.map(([label,value]) => <article key={label} className="rounded-xl border border-white/10 bg-white/[.03] p-5"><p className="text-xs uppercase tracking-wider text-zinc-500">{label}</p><p className="mt-2 text-2xl font-black text-white">{value}</p></article>)}</div><h2 className="mt-10 text-xl font-bold text-white">Últimas 10 donaciones</h2><div className="mt-4 overflow-x-auto"><table className="w-full min-w-[850px] text-left text-sm"><thead className="text-xs uppercase text-zinc-500"><tr>{["Fecha","Estado","Kg","Monto","Participante","Donante","Referencia"].map(h=><th key={h} className="border-b border-white/10 p-3">{h}</th>)}</tr></thead><tbody>{recent.map(d=><tr key={d.id} className="border-b border-white/5"><td className="p-3">{new Date(d.created_at).toLocaleString("es-AR")}</td><td className="p-3">{d.status}</td><td className="p-3">{d.impact_units}</td><td className="p-3">{formatCurrency(d.amount)}</td><td className="p-3">{d.participant}</td><td className="p-3">{d.donor}</td><td className="p-3 font-mono text-xs">{d.payment_reference ?? "—"}</td></tr>)}</tbody></table></div><Link href="/admin/donaciones" className="mt-4 inline-block text-sm text-amber-300">Ver todas →</Link></section>; }
